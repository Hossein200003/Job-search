import { createContext } from "react";

const Filters_context = createContext(null);
const Sort_context = createContext(null);
const Reverse_context = createContext(null);

export { Filters_context, Sort_context, Reverse_context };
